# MasterMind Harness — requirements
httpx>=0.27.0
python-dotenv>=1.0.0
llama-cpp-python>=0.2.90
chainlit>=1.0.0
keyring>=24.0.0
nbformat>=5.0.0
kokoro-onnx>=0.3.0
sounddevice>=0.4.6
soundfile>=0.12.1
pytest>=7.0.0

# Local network discovery (optional, enables /discover)
zeroconf>=0.131.0

# Vector embeddings for memory search (optional, pick one)
numpy>=1.24.0
sentence-transformers>=2.0.0   # EMBED_BACKEND=sentence_transformers (default)
# llama-cpp-python already listed above — used when EMBED_BACKEND=llama_cpp
